{
  "main": {
    "prefix": "main",
    "body": ["if __name__ == '__main__':", "    $1"],
    "description": "main"
  },
  "unittest": {
    "prefix": "test",
    "body": [
      "    def test_$1(self):",
      "        # given: ",
      "        $2",
      "",
      "        # when:",
      "",
      "        # then:"
    ],
    "description": "create unittest"
  },
  "test_class": {
    "prefix": "test_class",
    "body": [
      "from unittest import mock",
      "",
      "class Test$1:",
      "    @classmethod",
      "    def setup_class(cls):",
      "        cls.patcher = mock.patch($2)",
      "        mock = cls.kinesis_patcher.start()",
      "",
      "    @classmethod",
      "    def teardown_class(cls):",
      "        cls.patcher.stop()",
      "",
      "    def setup_method(self):",
      "        self.mock.reset_mock()",
      ""
    ],
    "description": "create unittest"
  }
}
