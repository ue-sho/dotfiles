# Ghostty config — read by cmux (Ghostty-based) and Ghostty itself.
# Dark theme with an opaque background.

# Font
font-family = Monaco
font-size = 12

# Built-in dark theme (palette/background/foreground come from here)
theme = Iceberg Dark
